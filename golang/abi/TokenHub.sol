pragma solidity 0.6.4;

contract TokenHub {
    function transferOut(address contractAddr, address recipient, uint256 amount, uint64 expireTime) external returns (bool) {
        return true;
    }
}
